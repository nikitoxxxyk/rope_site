Новый заказ #{ order.id }

Клиент: { order.customer_name }
Телефон: { order.phone }
Email: { order.email }

Товары:
{% for item in items %}
- { item.product.name } x { item.quantity } = { item.price * item.quantity } руб.
{% endfor %}

Итого: { total } руб.

Комментарий: { order.comment|default:"—" }
